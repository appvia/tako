version: "3.7"
services:
  db:
    image: mysql:8.0.19
    x-k8s:
      workload:
        replicas: 1
        livenessProbe:
          type: exec
          exec:
            command:
              - echo
              - I'm a useless check
  wordpress:
    x-k8s:
      workload:
        unknown: "unknown-value"
        replicas: 1
        livenessProbe:
          type: exec
          exec:
            command:
              - echo
              - I'm a useless check
volumes:
  db_data:
    x-k8s:
      storageClass: standard
